apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: paint-app-secret
  namespace: colombus
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: colombus-asm-store
    kind: ClusterSecretStore
  target:
    name: paint-app-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: colombus/paint
---
apiVersion: v1
kind: Service
metadata:
  name: paint-svc
  namespace: colombus
  labels:
    app: paint-svc
spec:
  type: ClusterIP
  selector:
    app: paint-svc
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: paint-svc
  namespace: colombus
  labels:
    app: paint-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paint-svc
  template:
    metadata:
      labels:
        app: paint-svc
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
        - name: paint-svc
          image: 970103397792.dkr.ecr.ap-northeast-2.amazonaws.com/colombus/paint:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT      # ★ Spring이 server.port 로 인식하는 환경변수
              value: "8080"          
          envFrom:
            - secretRef:
                name: colombus-global-secret
            - secretRef:
                name: paint-app-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
