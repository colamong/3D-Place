apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: report-app-secret
  namespace: colombus
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: colombus-asm-store
    kind: ClusterSecretStore
  target:
    name: report-app-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: colombus/report
---
apiVersion: v1
kind: Service
metadata:
  name: report-svc
  namespace: colombus
  labels:
    app: report-svc
spec:
  type: ClusterIP
  selector:
    app: report-svc
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-svc
  namespace: colombus
  labels:
    app: report-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: report-svc
  template:
    metadata:
      labels:
        app: report-svc
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
        - name: report-svc
          image: 970103397792.dkr.ecr.ap-northeast-2.amazonaws.com/colombus/report:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT      # ★ Spring이 server.port 로 인식하는 환경변수
              value: "8080"          
          envFrom:
            - secretRef:
                name: colombus-global-secret
            - secretRef:
                name: report-app-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
